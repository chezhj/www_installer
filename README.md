# www_installer

Custom scripts to install phython/django based web apps as
app.vdwaal.net

Run add_to_path once to add the build part to your path, so all projects can execute
Refresh by
git fetch origin

git reset --hard origin/main
